**Theorem.** The set of all *factorions* in base 10 is exactly $\{1, 2, 145, 40585\}$. That is, a natural number $n$ equals the sum of the factorials of its decimal digits if and only if $n \in \{1, 2, 145, 40585\}$.

More precisely, writing $n$ in base 10 as the sequence of digits $d_1, d_2, \ldots, d_k$ (each $d_i \in \{0,\ldots,9\}$), define

$$F(n) = \sum_{i=1}^{k} d_i!$$

Then $\{n \in \mathbb{N} \mid n = F(n)\} = \{1, 2, 145, 40585\}$.

*Proof.* Unsolved.